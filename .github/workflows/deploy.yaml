name: Deploy MultiDocker
on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t rakitenkogermans/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true rakitenkogermans/react-test npm test

      - run: docker build -t rakitenkogermans/multi-client ./client
      - run: docker build -t rakitenkogermans/multi-nginx ./nginx
      - run: docker build -t rakitenkogermans/multi-server ./server
      - run: docker build -t rakitenkogermans/multi-worker ./worker

      - run: docker push rakitenkogermans/multi-client
      - run: docker push rakitenkogermans/multi-nginx
      - run: docker push rakitenkogermans/multi-server
      - run: docker push rakitenkogermans/multi-worker
